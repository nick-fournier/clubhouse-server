# Secrets — never commit real env files (commit example.env instead)
.env
**/.env
*.dump

# OSRM compiled graph data (multi-GB, reproducible via orange/prep-data.sh)
**/data/

# Python / uv (thinkbox prep tooling) — env is reproducible from uv.lock
.venv/
__pycache__/
